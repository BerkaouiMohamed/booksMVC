<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    <h1><%= book.title %></h1>
    <h1><%= book.price %></h1>

    <div id="comments">
        <% book.comments.forEach(com => { %>
         <p><%= com.user %></p>
         <h3><%= com.comment %></h3>
        <% }) %>
    </div>

    <form action="/comment/<%= book._id  %>"  method="post">
        <h1>add new comment</h1>
        <input type="text" name="user" placeholder="name here">
        <input type="text" name="comment" placeholder="comment here">
        
        <input type="submit" value="submit">
    </form>

    <a href="/books">return</a>
</body>
</html>